# SASL Transformer dependencies
# Install with: pip install -r requirements.txt

# Anthropic SDK for Claude API
anthropic>=0.40.0

# FastAPI and async server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Data validation
pydantic>=2.9.0
pydantic-settings>=2.5.0

# WebSocket support (included with Fastapi but listed explicitly)
websockets>=13.0

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
httpx>=0.27.0

# ── HARPS sign-language recognition stack ────────────────────────────────────
# Core ML
torch>=2.0
numpy>=1.23
scipy>=1.10
scikit-learn>=1.2
tqdm>=4.64

# Visualisation and export
matplotlib>=3.7
openpyxl>=3.1
pandas>=1.5.3

# Path Signature Features (optional — HARPS falls back to flatten if unavailable)
# iisignature requires a C compiler; install separately if needed:
#   pip install iisignature
# iisignature>=0.24
