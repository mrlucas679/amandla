# AMANDLA — Python dependencies
# Install with: pip install -r requirements.txt
# All AI runs on local Ollama — no cloud API keys needed.

# FastAPI and async server
fastapi==0.115.0
uvicorn[standard]==0.32.0

# Data validation
pydantic==2.10.0
pydantic-settings>=2.0.0

# WebSocket support
websockets==13.1

# HTTP client (for Ollama API calls)
httpx>=0.27.0

# Environment variable loading
python-dotenv==1.0.1

# Async file I/O
aiofiles==24.1.0

# Multipart file uploads (speech audio)
python-multipart==0.0.6

# HTTP requests
requests>=2.32.0

# Audio format conversion (ffmpeg wrapper)
imageio-ffmpeg>=0.5.0

# Speech-to-text (local, no API key)
faster-whisper==1.1.0

# HARPS sign recognition
torch>=2.0
numpy>=1.23
scipy>=1.10
scikit-learn>=1.2
matplotlib>=3.7

# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
